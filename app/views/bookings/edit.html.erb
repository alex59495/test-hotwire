<%= turbo_frame_tag dom_id(@booking) do %>
  <%= simple_form_for [@booking.room, @booking] do |f| %>
    <%= f.input :start_date, as: :date %>
    <%= f.input :end_date, as: :date %>
    <%= f.submit "Validate", class: 'btn btn-primary' %>
  <% end %>
<% end %>