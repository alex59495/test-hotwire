<div class="card-booking">
  <%= turbo_frame_tag dom_id(booking) do %>
    <p>Réservation <%= booking.room.name %></p>
    <p>Du : <%= booking.start_date.strftime("%d%m%y") %></p>
    <p>Au : <%= booking.end_date.strftime("%d%m%y") %></p>
    <div class="d-flex">
      <%= link_to "Edit", edit_room_booking_path(booking.room, booking), class: 'btn btn-warning'  %>
      <%= button_to "Destroy", room_booking_path(booking.room, booking), method: :delete, class: 'btn btn-danger' %>
    </div>
  <% end %>
</div>